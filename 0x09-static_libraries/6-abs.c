#include "main.h"

/**
 * _abs - Will compute the absolute value of an integer
 * @n: is the integer value to be checked
 *
 * Return: The integer's absolute value.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
