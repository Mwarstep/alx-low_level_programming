#include <stdio.h>

/**
 * main - will print the sentence "Programming is like building a multilingual puzzle"
 * Return - 0 if exited, non-zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle ");
	return (0);
}
