#include <stdio.h>

/**
 * main - prints phrase with proper grammar
 * but the outcome is a piece of art,
 * return 0 if prog success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
